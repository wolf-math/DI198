// 1. Function Definition:
// Define a function called multiply that takes two parameters, x and y, both of type number, and returns their product.

function multiply(x: number, y: number): number {
  return x * y;
}

// 2. Parameter Type:
// Ensure the parameters x and y are annotated with the number type.

// 3. Return Type:
// The function should return a number which is the result of multiplying x and y.

// 4. Test the Function:
// Call the multiply function with two numbers, e.g., 3 and 4, and print the result to the console.

multiply(9, 345);
multiply(-98234, 0);
// multiply("23", "14")
